## Testing, documentation, Referencing and configuration
version: 2

models:
  - name: dim_locations
    description: " Table describing and detailing every location of an eviction on per row basis "
    columns:
      - name: location_id
        description: "The order Id of the order"
        tests:
          #- not_null
          #- unique