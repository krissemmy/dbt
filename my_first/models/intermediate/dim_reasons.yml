## Testing, documentation, Referencing and configuration
version: 2

models:
  - name: dim_reasons
    description: "Table showing reason for eviction"
    columns:
      - name: reason_id
        description: "The order Id of the reasons"
        tests:
          - not_null
          - unique